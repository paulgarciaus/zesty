apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-cert
  namespace: nginx
spec:
  secretName: nginx-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - nginx.local
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
